{
  "name": "@diplomat/desktop",
  "version": "0.38.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "check": "pnpm test && pnpm typecheck",
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "node -e \"const { spawnSync } = require('node:child_process'); const result = spawnSync('cargo', ['test', '--manifest-path', 'src-tauri/Cargo.toml'], { stdio: 'inherit', env: { ...process.env, DIPLOMAT_SKIP_TAURI_BUILD: '1' } }); process.exit(result.status ?? 1);\"",
    "typecheck": "node -e \"console.log('desktop shell has no TypeScript sources')\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "typescript": "^5.8.0"
  }
}
